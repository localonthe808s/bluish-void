# The trail is a measurement in progress, not a build product. A snapshot every
# five minutes across seven cities is ~2000 rows a day; committing it would bury
# the repo's history in machine noise for a study that lives on one laptop.
# Fold a slice in deliberately when there is a result worth keeping.
*.jsonl
agent.log
